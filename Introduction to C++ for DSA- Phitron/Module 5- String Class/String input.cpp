#include <bits/stdc++.h>
using namespace std;

int main() {
    string s; // Works like dynamic array, char s[10] -> static array
    // cin >> s;
    // cin.ignore(); // Use when input in multiple line
    getline(cin, s); // cin.getline(variable name, size) -> char
    cout << s << endl;
    return 0;
}